<h1>Typography</h1>
<p>In order to use Design System Typography, you need to import <code>/base/_typography.scss</code> partial in your SCSS file. It's recommended to use available typography variables to keep consistency across products.</p>

<section>
	<h2 id="font-family">Font Family</h2>
	<p>For highest browser compatibility we decided to follow direction described in <a href="https://www.smashingmagazine.com/2015/11/using-system-ui-fonts-practical-guide/">Using System UI Fonts In Web Design: A Quick Practical Guide</a>. It provides a list of font-families for browsers to fallback. We listed font-families supported by major operating systems and finally we fall back to <code>sans-serif</code>. This way we can avoid web-font delivery process which would hit page performance.</p>
	<code><span class="variable-name">$font-family</span>: 'San Francisco', 'Helvetica Neue', 'Roboto', 'Segoe UI', 'Ubuntu', 'Droid Sans', sans-serif;</code>
</section>

<section>
	<h2 id="line-height">Line height</h2>
	<code><span class="variable-name">$line-height</span>: 1.5em;</code>
</section>

<section>
	<h2 id="typescale">Typescale</h2>
	{{typescale-line fontSize=54 typescaleName='typescale-size-plus-7'}}
	{{typescale-line fontSize=48 typescaleName='typescale-size-plus-6'}}
	{{typescale-line fontSize=40 typescaleName='typescale-size-plus-5'}}
	{{typescale-line fontSize=36 typescaleName='typescale-size-plus-4'}}
	{{typescale-line fontSize=28 typescaleName='typescale-size-plus-3'}}
	{{typescale-line fontSize=24 typescaleName='typescale-size-plus-2'}}
	{{typescale-line fontSize=18 typescaleName='typescale-size-plus-1'}}
	{{typescale-line fontSize=16 typescaleName='typescale-size-base'}}
	{{typescale-line fontSize=14 typescaleName='typescale-size-minus-1'}}
	{{typescale-line fontSize=12 typescaleName='typescale-size-minus-2'}}
	{{typescale-line fontSize=10 typescaleName='typescale-size-minus-3'}}
</section>

<section>
	<h2 id="example">Example usage</h2>
	{{#code-sample}}
body {
	font-family: $font-family;
	line-height: $line-height;
}

.wds-component {
	font-size: $typescale-size-plus-1;
}
	{{/code-sample}}
</section>
