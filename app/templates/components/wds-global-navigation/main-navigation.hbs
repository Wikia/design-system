{{#each model as |item|}}
	{{!
		Componenets that can be rendere here are:
		- link-text
		- link-group
	}}
	{{component
		(concat 'wds-global-navigation/' item.type)
		model=item
		standaloneLink=true
	}}
{{/each}}
