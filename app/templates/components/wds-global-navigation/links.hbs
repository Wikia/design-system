{{#if model.header}}
	{{wds-global-navigation/dropdown
		dropdownRightAligned=dropdownRightAligned
		model=model
		type=type
	}}
{{else}}
	{{#each model.links as |link|}}
		{{wds-global-navigation/link
			model=link
			tagName='a'
			classNames='wds-global-navigation__link'
		}}
	{{/each}}
{{/if}}
