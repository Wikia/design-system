{{#wds-tabs as |tabs|}}
	{{#each model as |item|}}
		{{#tabs.tab data-tracking-label=item.tracking_label}}
			{{#if item.items}}
				{{wds-community-header/local-navigation-dropdown item=item}}
			{{else}}
				<div class="wds-tabs__tab-label">
					<a href="{{item.href}}">
						{{partial 'partials/nav-item-with-icon'}}
					</a>
				</div>
			{{/if}}
		{{/tabs.tab}}
	{{/each}}
{{/wds-tabs}}
