{{#each displayableAvatars as |avatar|}}
  <Avatar
    @src={{avatar.src}}
    @alt={{avatar.alt}}
    @link={{avatar.link}}
    @badge={{avatar.badge}}
  />

{{/each}}
{{#if overflow}}
  <div class="wds-avatar-stack__overflow">+{{overflow}}</div>
{{/if}}
