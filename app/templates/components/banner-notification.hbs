<div class="wds-banner-notification__icon">
  <Icon @name={{this.icon}} @size="small" />
</div>
<span class="wds-banner-notification__text">
  {{yield}}
</span>
<Icon
  @name="cross"
  @size="tiny"
  class="wds-banner-notification__close"
  @onClick={{action (optional @onClose)}}
/>
