<h1>Toggles</h1>
<p>
	Toggle is a control that is used to quickly switch between two possible states.
	All toggles require a label due to fact that styling inputs is tricky cross browser.
	All toggle inputs should have a unique id for label to work correctly.
</p>

<section>
	<h2 id="text-only">Simple toggle</h2>

	{{#component-demo}}
		<input type="checkbox" id="toggleA" class="wds-toggle__input">
		<label for="toggleA" class="wds-toggle__label"></label>
	{{/component-demo}}
</section>

<section>
	<h2 id="text-only">Toggle with a label</h2>

	{{#component-demo}}
		<input type="checkbox" id="toggleB" class="wds-toggle__input">
		<label for="toggleB" class="wds-toggle__label">Slide me if you wish</label>
	{{/component-demo}}
</section>

<section>
	<h2 id="text-only">Disabled toggle</h2>

	{{#component-demo}}
		<input type="checkbox" id="toggleDisabled" class="wds-toggle__input" disabled>
		<label for="toggleDisabled" class="wds-toggle__label">Yep I'm disabled</label>
	{{/component-demo}}

	<h2 id="text-only">Disabled and checked toggle</h2>

	{{#component-demo}}
		<input type="checkbox" id="toggleDisabledB" class="wds-toggle__input" disabled checked>
		<label for="toggleDisabledB" class="wds-toggle__label">Yep I'm disabled</label>
	{{/component-demo}}
</section>
