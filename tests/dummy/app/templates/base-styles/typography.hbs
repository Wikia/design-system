<h1>Typography</h1>
<p>
	As a timeless, highly effective font, Helvetica is at the forefront of the Fandom brand and used across our mobile
	and desktop site and is available in various weights. Our native apps inherit the respective default fonts —
	San Francisco on iOS and Roboto on Android — and also contain various weights.
</p>
<p>
	Note: the 10px size should be displayed in all caps only to ensure proper readability.
</p>
<p>
	It's recommended to use available typography variables to keep consistency across products.
</p>

<section>
	<h2 id="font-family">Font Family</h2>
	<p>For highest browser compatibility we decided to follow direction described in <a href="https://www.smashingmagazine.com/2015/11/using-system-ui-fonts-practical-guide/">Using System UI Fonts In Web Design: A Quick Practical Guide</a>. It provides a list of font-families for browsers to fallback. We listed font-families supported by major operating systems and finally we fall back to <code>sans-serif</code>. This way we can avoid web-font delivery process which would hit page performance.</p>
	<code><span class="format-code">$wds-font-family</span>: 'Helvetica Neue', Helvetica, Arial, sans-serif;</code>

</section>

<section>
	<h2 id='news-stories-font'>News and stories and Mobile Article font family</h2>
	<p> News and Stories and uses Rubik as it's primary font.</p>
	<code><span class="format-code">$wds-fandom-font-family</span>: Rubik, Helvetica, Arial, sans-serif;</code>
</section>

<section>
	<h2 id="typescale">Typescale</h2>
	{{typescale-line fontSize=52 lineHeight=52 typescaleName='font-size-4xl' lineHeightName='line-height-4xl'}}
	{{typescale-line fontSize=36 lineHeight=36 typescaleName='font-size-3xl' lineHeightName='line-height-3xl'}}
	{{typescale-line fontSize=28 lineHeight=30 typescaleName='font-size-xxl' lineHeightName='line-height-xxl'}}
	{{typescale-line fontSize=24 lineHeight=26 typescaleName='font-size-xl' lineHeightName='line-height-xl'}}
	{{typescale-line fontSize=18 lineHeight=20 typescaleName='font-size-l' lineHeightName='line-height-l'}}
	{{typescale-line fontSize=16 lineHeight=18 typescaleName='font-size-base' lineHeightName='line-height-base'}}
	{{typescale-line fontSize=14 lineHeight=16 typescaleName='font-size-s' lineHeightName='line-height-s'}}
	{{typescale-line fontSize=12 lineHeight=14 typescaleName='font-size-xs' lineHeightName='line-height-xs'}}
	{{typescale-line fontSize=10 lineHeight=12 typescaleName='font-size-xxs' lineHeightName='line-height-xxs'}}
</section>

<section>
	<h2 id="font-weight">Font Weight</h2>
	{{font-weight-line fontWeight=300 fontWeightName='font-weight-light'}}
	{{font-weight-line fontWeight=400 fontWeightName='font-weight-medium'}}
	{{font-weight-line fontWeight=500 fontWeightName='font-weight-normal'}}
	{{font-weight-line fontWeight=700 fontWeightName='font-weight-bold'}}
	{{font-weight-line fontWeight=900 fontWeightName='font-weight-black'}}
</section>

<section>
	<h2 id="font-weight">Text style</h2>
	{{text-style-line text='Normal' textStyle='font-style-normal'}}
	{{text-style-line text='Italic' textStyle='font-style-italic'}}
	{{text-style-line text='Uppercase' textStyle='text-transform-uppercase'}}
	{{text-style-line text='Lowercase' textStyle='text-transform-lowercase'}}
	{{text-style-line text='No transform' textStyle='text-transform-none'}}
</section>


<section>
	<h2 id="font-weight">Midlight</h2>
	<p>
		There's a specific styling for page titles, that you can apply by using a class
		<code><span class="format-code">.wds-midlight</span></code>
	</p>
	<div class="wds-font-size-xxl wds-font-weight-bold wds-midlight">This is a headline using midlight</div>
</section>

<section>
	<h2 id="example">Example usage</h2>
	{{#code-sample language='scss'}}
body {
	font-family: $wds-font-family;
	@include .wds-line-height-base;
}

.wds-component {
	@include wds-font-size-base;
}
	{{/code-sample}}
</section>
