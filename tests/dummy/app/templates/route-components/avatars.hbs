<h1>Avatars</h1>
<p>
  Avatars across Fandom should be circular with a 2px internal border.
</p>

<section>
  <h2 id="avatar-circle">Circle avatar</h2>

  <Demo @name="avatar">
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="admin"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @link="http://fandom.wikia.com"
    />
    <Avatar
      @src=""
      @alt=" user name"
    />
  </Demo>

  <h2>Parameters</h2>
  <div>
    <h3>HBS Properties</h3>
    <ul>
      <li><strong>@src</strong> (string): path to the the icon</li>
      <li><strong>@alt</strong> (string): alternative title</li>
      <li><strong>@badge</strong> (string): if provided, badge will be displayed on top of avatar</li>
      <li>
        <strong>@link</strong> (string): if provided, avatar will be wrapped in
        <code class="format-code">&lt;a&gt;</code> tag and linked to url provided as value.
      </li>
    </ul>
  </div>

  <h2>Sizes and theming</h2>
  <p>
    Size and colors of avatar can be set via SCSS mixin <code class="format-code">wds-avatar</code>, for example
    <Demo @name="avatars-scss" language="scss">
      .wds-avatar {
        @include wds-avatar(30px, $color, $color-hover, $color-background);
      }
    </Demo>
  </p>
  <div class="assets-wrapper">
    <div id="avatar-24" class="asset_box wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">24px</div>
    </div>

    <div id="avatar-30" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">30px</div>
    </div>

    <div id="avatar-36" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">36px</div>
    </div>

    <div id="avatar-42" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">42px</div>
    </div>

    <div id="avatar-48" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">48px</div>
    </div>

    <div id="avatar-60" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">60px</div>
    </div>

    <div id="avatar-100" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">100px</div>
    </div>

    <div id="avatar-150" class="wds-avatar-style-demo">
      <div class="asset-box__asset">
        <Avatar
          @alt="user name"
          @badge="admin"
        />
      </div>
      <div class="asset-box__name">150px</div>
    </div>
  </div>
</section>

<section>
  <h2>Badges</h2>
  <Demo @name="avatar-with-badge">
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="admin"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="content-moderator"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="discussion-moderator"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="global-discussions-moderator"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="helper"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="staff"
    />
    <Avatar
      @src={{concat rootURL "images/FANDOM-Avatar.jpg"}}
      @alt="user name"
      @badge="soap"
    />
  </Demo>
</section>

<section>
  <h2 id="avatar-circle">Avatar stack</h2>
  <p>
    To display an avatar stack, wrap all of avatars in <code class="format-code">.wds-avatar-stack</code>.
  </p>

  <Demo @name="avatar-stack">
    <AvatarStack @avatars={{avatars}} />
  </Demo>

  <h2>Parameters</h2>
  <div>
    <h3>HBS Properties</h3>
    <ul>
      <li><strong>@avatars</strong> (array): array of avatars</li>
      <li><strong>@maxStackSize</strong> (int): if provided, it will change the number of displayed avatars</li>
      <li><strong>@overrideCount</strong> (int): if provided, it will override the avatars array length to be used in the overflow text</li>
      <li><strong>@hideOverflow</strong> (boolean): hide the overflow text if needed</li>
    </ul>
  </div>
</section>
